// lifter config
  // set up YML file
  // check if it exists

    // if yes
      // Overwrite current YML file Y/N
        // if yes
          // ask our questions
        // if no
          // exit with message about what they should do
    // if no
      // ask our questions

  // build YML file when questions are completed

  // message: enter "lifter init" to initialize container

// lifter init
  // object of answers
  // boot2docker init
    // check response
      // if exists,
        // keep going
      // if not
        // pull it down
  // boot2docker up
  // check for dockerhost at /etc/hosts
  //